# Processing worker deps — the heavy ML stack (why this can't be a zip Lambda).
# CPU-only torch keeps the image within Lambda's 10 GB container limit.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.1
mediapipe==0.10.14
numpy<2.0
pandas>=2.0,<3.0
opencv-python-headless==4.10.0.84
pyarrow>=15
anthropic>=0.111,<1.0
boto3>=1.34
matplotlib>=3.8,<4          # render_scorecard.py (scorecard PNG in the scorecard step)
imageio-ffmpeg>=0.4.9       # bundles a static ffmpeg — overlay.mp4 must be H.264 for Chrome
# vendored GolfPose common/ (MixSTE2) model code — versions mirror the local venv
einops>=0.8,<0.9
timm>=1.0,<1.1
local-attention>=1.11,<2
